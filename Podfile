# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'RiR_POC' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'IBMWatsonTextToSpeechV1', '~> 2.1.0'

  # Pods for RiR_POC

  target 'RiR_POCTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'RiR_POCUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
